FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY agent.py .

EXPOSE 8080

ENV PORT=8080
ENV AGENT_NAME="DevOps Copilot"

CMD ["python", "agent.py"]
